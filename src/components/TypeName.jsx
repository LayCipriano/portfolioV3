import { ReactTyped } from "react-typed"

export const TypeName = () => {
    return (
        <ReactTyped
            strings={[
                "frontend",
                "testes",
                "requisitos",
                "automação"
            ]}
            typeSpeed={40}
            backSpeed={50}
            loop
        />
    )
}